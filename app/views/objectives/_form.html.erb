<%= form_for(@objective, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @objective.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@objective.errors.count, "error") %> prohibited this objective from being saved:</h4>

      <ul>
      <% @objective.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :name, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :section, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :section, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :percentage, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :percentage, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
